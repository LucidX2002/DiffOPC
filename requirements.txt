# Canonical pip dependencies for the current runnable DiffOPC workspace.
# Supported and validated Python version: 3.11.
# For a machine-specific lock snapshot, run `bash export_dopc_env.sh`.

# --------- pytorch --------- #
torch==2.7.0
torchvision==0.22.0
lightning==2.6.1
torchmetrics==1.9.0

# --------- hydra --------- #
hydra-core==1.3.2
hydra-colorlog==1.2.0
hydra-optuna-sweeper==1.2.0

# --------- loggers --------- #
# wandb
# neptune-client
# mlflow
# comet-ml
aim==3.29.1

# --------- others --------- #
adaptive-boxes==0.0.4  # provides the adabox module used by OPC evaluation and MRC code
matplotlib==3.10.3
numpy==1.26.4
opencv-python==4.11.0.86
packaging==25.0
pillow==11.2.1
pytest==9.0.2
rich==14.3.3
rootutils==1.0.7       # standardizing the project root setup
scipy==1.15.3
# pre-commit is not part of the current runnable dopc baseline. Install it separately for `make format`.
# sh            # for running bash commands in some tests (linux/macos only)
