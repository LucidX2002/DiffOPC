# Canonical conda entrypoint for this repository.
# The supported and currently validated interpreter version is Python 3.11.

name: dopc

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip>=25
  - pip:
      - -r requirements.txt
